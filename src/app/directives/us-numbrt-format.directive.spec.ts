import { UsNumbrtFormatDirective } from './us-numbrt-format.directive';

describe('UsNumbrtFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new UsNumbrtFormatDirective();
    expect(directive).toBeTruthy();
  });
});
